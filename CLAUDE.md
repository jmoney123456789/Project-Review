# Project Preferences

## UI/UX Preferences

- **Never truncate text with ellipsis** - Always allow text to wrap and show full content. The user prefers seeing full titles/names rather than having them cut off with "...". Use `word-wrap: break-word` and `overflow-wrap: break-word` instead of `text-overflow: ellipsis`.
